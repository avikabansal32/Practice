import random
import string

chracter_values = string.ascii_letters + string.digits + string.punctuation
pas_length = 12

password = ""
for i in range(pas_length):
    password += random.choice(chracter_values)

print("your password is:",password)